import CartClient from "./CartClient";

export const metadata = {
  title: "Cart - Kupliq Cafe",
  description: "Keranjang Belanja",
};

export default function CartPage() {
  return <CartClient />;
}
